import React from 'react';
export class Endpoints extends React.Component {

    endpoints = () => {
        return {
            setTypes: '/sets/types',
            setUnits: '/sets/units',
            workoutSession: '/sets/workout/'
        }
    };

    //move to app config

}

